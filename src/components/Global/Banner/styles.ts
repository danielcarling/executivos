import { styled } from "styled-components";

export const BannerWrapper = styled.div`
   display: flex;
   justify-content: center;
   margin: 0 2rem;

   img {
      max-width: 90rem;
      width: 100%;
      height: auto;
      margin-top: 0.2rem;
   }
   `